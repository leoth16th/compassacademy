name: publish-cycle

'on':
  # Dispatch at 04:30, 05:00, 05:30 UTC (08:00/08:30/09:00 Tehran) - GHA cron
  # is best-effort and silently skipped the 2026-09-19 run entirely, so one
  # shot/day isn't reliable. 3 tries within 1hr of the intended 08:00 Tehran
  # slot as a safety net; dispatch_due.py only sends items whose scheduled
  # time has passed and are still state=staged, so re-running is a safe
  # no-op once the real send has already gone through.
  schedule:
    - cron: '30 4 * * *'
    - cron: '0 5 * * *'
    - cron: '30 5 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r senders/requirements.txt

      - name: Send anything due
        env:
          TELEGRAM_BOT_TOKEN_1: ${{ secrets.TELEGRAM_BOT_TOKEN_1 }}
          TELEGRAM_BOT_TOKEN_2: ${{ secrets.TELEGRAM_BOT_TOKEN_2 }}
          BALE_BOT_TOKEN: ${{ secrets.BALE_BOT_TOKEN }}
        run: python senders/dispatch_due.py --tz Asia/Tehran

      - name: Commit updated state
        run: |
          git config user.name "compass-bot"
          git config user.email "bot@compassenglish.ir"
          git add control.db cycle/
          if ! git diff --quiet --cached; then
            git commit -m "chore: mark due items posted [skip ci]"
          fi

      - name: Push commits (fail job if push fails and there were changes)
        run: |
          git config user.name "compass-bot"
          git config user.email "bot@compassenglish.ir"
          CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
          # Check if there are commits to push (compare HEAD with remote)
          if git rev-parse --verify origin/$CURRENT_BRANCH >/dev/null 2>&1; then
            LOCAL=$(git rev-parse HEAD)
            REMOTE=$(git rev-parse origin/$CURRENT_BRANCH)
            if [ "$LOCAL" != "$REMOTE" ]; then
              echo "Local commits ahead of remote — pushing..."
              git push
              echo "✅ Push succeeded"
            else
              echo "No local commits to push (already in sync)"
            fi
          else
            echo "No upstream branch — pushing to establish tracking"
            git push -u origin $CURRENT_BRANCH
            echo "✅ Push succeeded"
          fi
