I'll start by mapping the workspace and reading the attachment plus the two referenced files, then do a deeper sweep for the technical assets (package.json, prompts, MCP configs, env examples, test procedures) before writing the report.
I now have a complete picture. Let me write the migration inventory package — a technical map Goose can use to verify against source, not a decorative summary.
Done. I swept the full workspace **and** the agent workspaces under `C:\Users\morte\.openclaw-autoclaw\agents\` (where the actual JS source and agent configs live — not just the main repo), then built a technical inventory package for Goose.

## What I found (verified against source, not summarized from memory)

**The two reference files**
- `AUTOCLAW_REPURPOSE_SPEC_v2.md` → ✅ exists at `calendar\`, and its two-agent model (main = operator/publisher, content = creator) is reflected in the report.
- `multi_agent_architecture_claude_autoclaw_goose_omni.md` → ❌ **does not exist** anywhere. Recursive filename search came up empty. Treat it as stale.

**Agents** (from `openclaw.runtime.json`): 6 configured — `main` (calendar operator + publisher, has cron+message), `content` (creator, `tools.deny:["cron"]`, no message tool), `js` (carousel source owner), `coder`, `auto-designer`, `secretary`. Plus 3 orphaned/archived (`auto-legal`, `auto-legal-global`, `auto-smb`).

**JavaScript/Node projects**: only **one real app** — the `ig-carousel-builder` (React 18 + Vite 5 + Express 4, port 4000), which exists in two states (canonical source in `agents\js\workspace`, deploy artifact in `agents\content\workspace\projects\...` with `src/` removed by design). Plus a retired `manager-gate` plugin and `ielts-ppt` (Playwright + pptxgenjs).

**MCP**: 5 servers — `fetch`, `filesystem` (stdio/npx), `autoclaw-productivity` (streamable-http `:19681` — Gmail/Calendar/Workspace/MS365/Notion), `autoclaw-github` (disabled), `notion` (OAuth).

**Cron**: 3 active recurring (Mon handoff-read, Thu 9AM reminder, Fri dead-man switch) + 5 disabled historical REAL1 posts.

**Gaps flagged for migration**: the Hugo blog pipeline lives **outside** this repo (`C:\Site\publisher\Publisher_V_5\` → FTP → `C:\Site\hugo-site-2-main`); Seedream image-gen depends on a local token service (`:18432`); there are **zero `.env` files** — Bale/Telegram tokens are hardcoded in scripts; and `hub/` + `carousel-inputs/` + `bale_sender.py` are **duplicated** between the main and content workspaces (Goose must reconcile which is canonical).

The package tells Goose to use the reports as a map and verify every claim against source — it deliberately does **not** include secrets, logs, caches, browser profiles, or the archived house-cleaning scratch.

One thing I could not fully resolve without a closer diff: **which of the two `hub/` copies is canonical** (main workspace vs `agents\content\workspace\hub`). The calendar spec points at the main workspace, but `HUB_OPERATIONS.md` (content side) points at its own. I flagged it in `01` and `08` — Goose should verify before rebuilding. Want me to diff those two trees next?

the files are not here but the address is : C:\Users\morte\.openclaw-autoclaw\workspace\goose-migration
i recommend you ceck the whole folder of openclase autoclaw ! and summarize what you learn out of them ! for your memory ! 
then 

All 8 files present now. Let me hand them off.That's the whole system, buildable now: 8 files — `GOOSE_BUILD_BRIEF.md` is what you literally run through Goose (`goose run -i GOOSE_BUILD_BRIEF.md -s`) to scaffold everything; the rest are the real recipes, schema, workflow, and sender skeleton it puts in place.

**How it maps to what you asked for:**
- Run Goose, it tells you where things stand → `cycle-status` recipe, read-only, queries `control.db`.
- "These are the things you have to approve" → `cycle-intake` recipe. It loads a manifest **Claude already wrote**, shows you each item near-verbatim, and only accepts approve/reject — it never touches the wording.
- "Now go do the manual postings, I'll schedule the automated ones" → `cycle-close` recipe splits approved items into manual vs automated, prints your manual checklist, and stages automated ones into `cycle/<week>/` in git.
- "Send the files to git so it works even if my system is off" → that's the point of staging into git: `.github/workflows/publish-cycle.yml` polls every 15 minutes on GitHub's servers, not yours, and fires `dispatch_due.py` — same idea as your Python setup now, just running somewhere that doesn't depend on your laptop being on.
- "Delete after the week, keep local history, don't let git get fat" → `cycle-archive` recipe: copies the week's folder to a local, gitignored archive path, then `git rm -r`s it from the repo.
- "Goose does labor, you do the ideas" → every recipe's instructions open with that rule explicitly and refuse to draft anything. The manifest format (in the brief) is the handoff contract between us — I write it, Goose only ever consumes it.

**Three things I flagged in the brief as open, not decided for you:**
1. Whether GitHub Actions scheduled workflows actually run reliably from your account — Iran-based accounts have hit GitHub restrictions before. Test this before trusting it as your "works even when I'm off" mechanism. If it doesn't work, the fallback is any always-on host running the same script — the design doesn't change either way.
2. `control.db` tracked directly in git vs. a plain-text `state.json` export alongside it — SQLite binary diffs are noisy in `git log`. Your call, tell Goose which you want.
3. `senders/telegram_send.py` and `bale_send.py` are stubs — point Goose at wherever your current Python senders actually live so it ports the real logic in, not fake it.

Hand Goose the brief and let it tell you what it hit.


the files are here 
now report to me what your plan is to execute this ! 